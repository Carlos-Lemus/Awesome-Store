document.getElementById("precio_").onchange = function (){

  var n = document.getElementById("precio_").value;

   if (isNaN(n)){
        alert ("Alerta! " + n + " no es un número. Ingrese un valor Válido.");
    } else {
    }
}

document.getElementById("stock_").onchange = function (){

  var n = document.getElementById("stock_").value;

   if (isNaN(n)){
        alert ("Alerta! " + n + " no es un número. Ingrese un valor Válido.");
    } else {
    }
}

document.getElementById("Nprecio_").onchange = function (){

  var n = document.getElementById("Nprecio_").value;

   if (isNaN(n)){
        alert ("Alerta! " + n + " no es un número. Ingrese un valor Válido.");
    } else {
    }
}

document.getElementById("Nstock_").onchange = function (){

  var n = document.getElementById("Nstock_").value;

   if (isNaN(n)){
        alert ("Alerta! " + n + " no es un número. Ingrese un valor Válido.");
    } else {
    }
}
